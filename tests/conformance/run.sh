#!/usr/bin/env bash
# Placeholder until t03-conformance-golden replaces it. Skips (exit 0) so early
# tasks are not blocked, but says so loudly -- a silent skip in a conformance
# gate is how a port convinces itself it is correct.
echo "conformance: SKIPPED (harness not built yet -- see t03-conformance-golden)"
exit 0
